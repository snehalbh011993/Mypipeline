stages:
    - build
    - test
    - deploy
build:
    stage: build
    script: 
        - echo "Building"
        - mkdir build
        - echo "hello" > build/info.txt
    artifacts: 
        paths:
            - build/
test:
    stage: test
    script: 
        - echo "Testing"
        - test -f "build/info.txt"
deploy:
    stage: deploy
    script:
        - echo "deployment is successful"